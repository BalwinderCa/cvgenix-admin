export const metadata = {
  title: 'Settings - Dashcode Next Js',
  description: 'Settings - Dashcode is a popular dashboard template.',
}

const layout = ({ children }) => {
  return (
    <>
      {children}
    </>
  );
};

export default layout;

