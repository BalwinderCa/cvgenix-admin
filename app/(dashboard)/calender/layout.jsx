export const metadata = {
  title: 'Calendar - Dashcode Next Js',
  description: 'Calendar - Dashcode is a popular dashboard template.',
}

const layout = ({ children }) => {
  return (
    <>
      {children}
    </>
  );
};

export default layout;

